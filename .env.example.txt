# Рекомендуемый шаблон локального окружения.
# Исходный документ требует .env.example, но не фиксирует точные
# имена переменных. Имена ниже нужно согласовать с реализацией.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=repair_control
DB_USER=repair_control
DB_PASSWORD=<set-locally>

BACKEND_PORT=8080
FRONTEND_PORT=<set-locally>

UPLOAD_DIR=./uploads

